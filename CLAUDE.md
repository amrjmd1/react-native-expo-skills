This repository hosts installable plugin packs under `plugins/`.
Each plugin contains one or more skills in `plugins/<plugin-name>/skills/<skill-name>/SKILL.md`.
When editing a skill, keep frontmatter concise and ensure description includes trigger contexts.
